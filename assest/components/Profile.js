import React  from "react";
import { View, StyleSheet, Text, Dimensions } from "react-native";

export const windowHeight = Dimensions.get("window");
export const screenWidth = Dimensions.get("screen");


const Screens = () => {

  return (
    <View>

    </View>
  );
}

export default Screens;